name: Simple Node.js application CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm ci

      # 4️⃣ Run lint (optional)
      - name: Run lint
        run: npm run lint --if-present

      # 5️⃣ Run tests (optional)
      - name: Run tests
        run: npm test --if-present

      # 6️⃣ Build Docker image
      - name: Build Docker image
        run: docker build -t express-hello .

    # # 7️⃣ (Optional) Push Docker image to Docker Hub
    # - name: Log in to Docker Hub
    #   uses: docker/login-action@v2
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}

    # - name: Push Docker image
    #   run: |
    #     docker tag express-hello your-dockerhub-username/express-hello:latest
    #     docker push your-dockerhub-username/express-hello:latest
